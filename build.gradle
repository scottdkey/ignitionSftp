plugins {
  kotlin("jvm") version "1.4.10"
  id("io.ia.sdk.modl") version "0.1.0-SNAPSHOT-12"


}
//
//dependencies {
//  implementation("com.inductiveautomation.gradle:ignition-module-plugin:1.2.10-SNAPSHOT")
//}
version  = "1.0.0"
group = "com.skm"
extra["sdkVersion"] = "8.1.0-SNAPSHOT"



ignitionModule {
    name.set("skmSFTP_${project.version}")
    fileName.set("skmSFTP")

    moduleName "skmSFTP_${project.version}"
    id.set( "com.skm.sftp")
    moduleVersion.set("$project.version}")
    moduleDescription.set("adding SFTP script functionality to ignition")
    requiredIgnitionVersion.set("8.1.0")
    isFree.set(true)
    license.set("license.html")
    moduleDependencies.set(mapOf(
        "com.inductiveautomation.perspective" to "C",
        "com.inductiveautomation.perspective" to "D",
        "com.inductiveautomation.perspective" to "G",
    ))
    hooks.set(mapOf("com.skm.sftp.client.ClientHook" to "C",
      "com.skm.sftp.gateway.GatewayHook" to "G",
      "com.skm.sftp.designer.DesignerHook" to "D"
    ))
  }
  tasks.getByName("signModule").enabled = true
allprojects{ tasks { withType<KotlinCompile> { kotlinOptions {
          jvmTarget = "11"
        }
      }
    }
  }


